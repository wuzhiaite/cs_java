server:
  port: 8001
  servlet:
    context-path: /

spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    cache: true
  profiles:
    active: dev

mybatis:
  configuration:
    cache-enabled: true

